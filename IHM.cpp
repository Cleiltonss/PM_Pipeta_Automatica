#include "mbed.h"
#include "IHM.h"

void IHM(int step_jog, int *xcount, int *ycount, int *zcount){
    if(step_jog == 0) {
        // EMITIR PRIMEIRA MENSAGEM: "APERTE VERDE PARA DEFINIR POSIÇÃO COLETA"
        if (*xcount != 0 && *ycount != 0 && *ycount != 0) {

        }
    }
}
